import { PlaceholderScreen } from '@/ui/components/PlaceholderScreen';

export default function SettingsScreen() {
  return <PlaceholderScreen testID="screen-settings" title="設定" message="設定画面は未実装です。" />;
}
