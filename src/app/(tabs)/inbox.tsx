import { PlaceholderScreen } from '@/ui/components/PlaceholderScreen';

export default function InboxScreen() {
  return <PlaceholderScreen testID="screen-inbox" title="未分類" message="未分類の一覧は未実装です。" />;
}
