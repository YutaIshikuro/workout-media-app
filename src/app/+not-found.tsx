import { PlaceholderScreen } from '@/ui/components/PlaceholderScreen';

export default function NotFoundScreen() {
  return <PlaceholderScreen testID="screen-not-found" title="ページが見つかりません" message="指定された画面は存在しません。" />;
}
