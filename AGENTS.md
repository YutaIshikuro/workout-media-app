# AGENTS.md

このファイルはこのリポジトリで作業するすべての AI エージェント（Codex / Claude Code / TAKT 配下の Worker・Review）が守る制約の**正本**である。
`CLAUDE.md` と `.kiro/steering/tech.md` はこのファイルを参照するだけで、制約を重複して書かない。制約を変更するときはこのファイルだけを編集する。

---

## 0. 最重要の設計制約

**ビジネスロジックを React に依存しない純粋関数として書くこと。**

この制約が本プロジェクトの検証戦略そのものである。開発機は Windows のみで Mac がなく、iOS 実機での目視確認は人間の手作業に限られる。React に絡んだロジックは Windows 上でテストできず、検証されないまま積み上がる。

- 種目サジェストのランキング、グルーピング・絞り込み・検索のクエリ組み立て、参照切れの判定、タグの多対多操作、日付グルーピング — これらは **`import` に `react` / `react-native` / `expo-*` を含まないモジュール**として書く
- React コンポーネントは、純粋関数が返した値を描画し、ユーザー操作を純粋関数に渡すだけにする
- `expo-sqlite` / `expo-media-library` / `expo-video` に触るコードは薄いアダプタに閉じ込め、その先のロジックはアダプタが返したプレーンなデータに対して動かす
- 判断に迷ったら「この関数は Node の `vitest` から呼べるか」を基準にする。呼べないなら分割が足りていない

---

## 1. プロダクト概要

iPhone の写真ライブラリに埋もれた筋トレの参考動画（Instagram / YouTube のフォーム解説の画面録画）を、種目別に整理して数タップで取り出せるようにする iOS アプリ。

- **トレーニングログではない。** 重量・回数・セット数を記録する機能は作らない
- **参考メディアのカタログである。** 種目によるグルーピングが主軸、日付は副次的
- 差別化はこの 3 点に限られる。判断に迷ったらここに立ち返る:
  1. 1 本のメディアを複数種目に同時所属させられる
  2. 未分類キューによる連続タグ付けの速さ（タグ付けの摩擦がアプリの生死を決める）
  3. フォーム確認プレイヤー（ループ / 速度変更 / コマ送り）
- 標準の写真アプリはフォルダ > アルバムの 2 段構造を持つ。「部位 → 種目の階層」だけでは差別化にならない
- MVP のユーザーは開発者 1 人。分類の客観的な正しさは要件ではない

---

## 2. 技術スタック（バージョン固定）

| 項目 | 採用 |
|---|---|
| Expo SDK | **57.0.18 以降**（57.0.9 でメモリ、57.0.17 で dev 起動遅延のリグレッション修正） |
| React Native | 0.86（SDK 57 同梱） |
| React | 19.2 |
| ルーティング | expo-router（ファイルベース） |
| 永続化 | expo-sqlite + Drizzle ORM |
| 動画 | expo-video |
| 写真ライブラリ | expo-media-library |
| 画像描画 | expo-image |
| ユニットテスト | vitest（Node 上、Windows で動くこと） |
| E2E | Maestro（GitHub Actions の macOS ランナー上、iOS Simulator） |
| 言語 | TypeScript strict |

New Architecture は SDK 55 以降で必須化されており無効化できない。すべてのコードが New Architecture 上で動くこと。

**Expo 系パッケージは SDK と同じバージョン系列に揃える。** SDK 57 では `expo-router` も `expo-status-bar` も `57.x` である（`expo-router@6.x` は旧系列であり、混ぜると peer 依存が解決できなくなる）。依存を追加・変更したら **`npx expo install --check` が `Dependencies are up to date` を返すことを確認する**。追加は `npm install` ではなく `npx expo install` を使う。

この確認を怠って `expo-router@6.0.24` が入ったまま 5 人のレビューと 5 回の検証をすり抜けた実例がある。型検査もテストも通るため、機械的に確認する以外に検出手段がない。

---

## 3. 禁止 API・使ってはいけないもの

**以下を新規コードに書いてはならない。既存コードに見つけたら報告すること。**

- `expo-av` の video API — 非推奨、Expo Go から削除済み。動画は必ず `expo-video`
- `expo-video-thumbnails` — SDK 57 でまだ動くが公式が非推奨としている。サムネイルは `expo-video` の `VideoPlayer.generateThumbnailsAsync()` を使う
- **`expo-media-library` の旧 options-bag API** — `getAssetsAsync` / `getAlbumsAsync` / `createAssetAsync` / `getAssetInfoAsync` などをルートから呼ぶと**実行時に throw する**。56.0.5 でオブジェクト指向 API がルートに昇格し、旧 API は `expo-media-library/legacy` に移動した
  - 正: `new Query().limit(n).eq(AssetField.MEDIA_TYPE, MediaType.VIDEO).exe()` / `Asset.create()` / `Album.get()` / `asset.getInfo()`
  - 数千件のメタデータ一括取得には SDK 57 の `Query.exeForMetadata()` を使う
  - `expo-media-library/legacy` からの import も禁止
- `expo-media-library` の `/next` サブパス — **もう存在しない**。ルートから import する
- トレーニングログ（重量・回数・セット数・RM 計算）に関する一切のコード・テーブル・画面

## 4. API 上の既知の落とし穴

- **コマ送りに `seekBy()` を使わない。** 公式ドキュメントが「フレーム精度が必要なら `currentTime` を使え」と明記しており、`seekBy` はオーバー/アンダーシュートする報告がある。`player.currentTime += step` で実装する
- `expo-video` に**動画の fps を返す API は存在しない**。コマ送りのステップは固定値の仮定（例: 1/30 秒）とし、その値をコードのコメントに根拠とともに書く
- `seekTolerance` のデフォルトは `0`（= 正確な位置へシーク）。変更する場合は理由をコメントに書く
- 高頻度スクラブには `scrubbingMode` を使う。iOS ではスクラブ中に pause し、ドラッグ終了時に `scrubbingModeEnabled` を false に戻す
- `generateThumbnailsAsync()` が返すのは `SharedRef<'image'>` であって file URI ではない。`expo-image` の `source` には直接渡せるが、ファイルパスとして保存はできない（本プロジェクトはファイル保存しない方針。§6 参照）
- 写真ライブラリの `localIdentifier` は **iCloud 復元や iOS 更新をまたぐと切れる**。恒久的な外部キーとして信頼してはならない

---

## 5. テスト戦略（3 層）

| 層 | 実行場所 | 所要 | 守備範囲 |
|---|---|---|---|
| ① ユニット | Node / Windows / vitest | 秒 | ロジック全般。**エージェントの主フィードバックループ** |
| ② Simulator E2E | GitHub Actions macOS ランナー + Maestro | 15 分 | 権限フロー、システムピッカー経由の取り込み、タグ付けキュー、一覧ナビゲーション、空状態。**回帰検出であって正しさの証明ではない** |
| ③ 実機目視 | 開発者の iPhone | 人間の手作業 | 動画再生の実描画、コマ送りの体感、スクロール性能、片手操作 |

**層 ② の構造的な盲点を必ず意識すること。** iOS Simulator は HEVC/H.265 のハードウェアデコードを持たず、映像が真っ黒になる。iPhone の画面録画は既定で HEVC の `.MOV` であり、**本アプリの主対象コンテンツそのものが Simulator では再生できない**。CI のシードメディアは H.264/AAC の `.mp4` に限る。**「E2E が通った = 動画再生が動く」ではない。**

E2E の前提処理（省略すると失敗する）:
1. `xcrun simctl bootstatus <udid> -b` で boot 完了を待つ
2. `xcrun simctl openurl booted photos-redirect:` で Photos.app を先に起動する（未初期化だと次で `PHPhotosErrorDomain error -1`）
3. `xcrun simctl addmedia booted <H.264 mp4...>` でシード
4. Maestro の `launchApp` に `permissions: { photos: limited }` を渡す（`limited` が指定可能）

**完了を主張する前に、必ず実際にテストを実行して出力を確認すること。** 「通るはず」は完了ではない。

---

## 6. アーキテクチャ上の決定（変更する場合は人間の承認が必要）

- **参照型カタログ。** メディア本体をアプリ内にコピーしない。写真ライブラリのアセット ID とメタデータのみを SQLite に保持する
- **自動再照合を実装しない。** 参照切れは検出して一覧に印を出し、ユーザーが「選び直す」でシステムピッカーから選び直したら**既存のタグをそのまま新しいアセットに移す**。作成日時・再生時間・ファイル名による自動同定ロジックは書かない（テスト不能なため）。ただしそれらのメタデータは後から遡って取得できないので**スキーマには最初から保持する**
- **取り込みの入口はシステムピッカーに統一する。** iOS の限定アクセス（`accessPrivileges: 'limited'`）を縮退状態ではなく主経路として設計する。「数千件から筋トレ動画を選ぶ UI」を自前で作らない
- **サムネイルをファイルとして永続化しない。** `generateThumbnailsAsync()` の結果を `expo-image` に直接渡し、ディスクキャッシュは `expo-image` に任せる。DB にサムネイルパス列を持たず、キャッシュの失効・容量管理も実装しない
- **`is_preset` のような区別を持たない。** 種目のシードは初回起動時に一度だけ投入される初期データであって種別ではない。投入後は全種目がユーザーのもので、名称・部位の変更も削除も自由。復旧手段は設定の「初期種目を再投入」のみ（名前が衝突するものはスキップ）
- **種目は主働部位 1 つに所属する**（1 対多）。メディア ↔ 種目は多対多
- 種目は日本語名 + 英字エイリアスを持つ。エイリアスは**スペース区切りの単一テキスト列**に入れ `LIKE` で検索する（数十〜百数十件規模では正規化も FTS5 も過剰）。表示は日本語名のみ
- 永続化層は、アセットの実体の在り処（写真ライブラリ参照か将来のアプリ内コピーか）に依存しないリポジトリ API を公開する。上位層は SQL を直接書かない

---

## 7. スコープ外（提案も実装もしないこと）

- トレーニングログ機能（重量・回数・セット数・RM 計算）
- AI / ML による種目の自動判定
- クラウド同期、アカウント、複数端末対応、バックエンド
- SNS 共有・ソーシャル機能
- 動画の編集・トリミング・書き出し・注釈描画
- 2 動画の並列比較再生
- Android 対応
- 外部リンク（YouTube / Instagram の URL）の収集
- 統計・分析ビュー
- お気に入り / コレクション
- 種目の並べ替え UI

**外部へのデータ送信をゼロに保つこと。** アナリティクス、クラッシュレポート、テレメトリを含め、ネットワーク送信を行うコードを追加しない。App Privacy を「データ収集なし」で申告できる状態を維持する。

---

## 8. リポジトリと秘密情報

このリポジトリは **public** である。

- 実際の写真・動画（個人のもの）をコミットしない。CI のシードメディアは合成した H.264 mp4 のみ
- `.env`、Apple ID、パスワード、API キー、証明書、プロビジョニングプロファイルをコミットしない
- Apple Team ID や ASC App ID は秘密情報ではないが、必要に応じて EAS Secrets に逃がす
- bundle identifier は **`com.yutaishikuro.formcatalog`** で固定。**変更するとアプリのデータコンテナが変わり、ユーザーが手で付けたタグが全部失われる。** 変更提案をしないこと

---

## 9. Git の運用

**`main` に直接コミットしない。作業は必ず最新の `main` から切ったブランチで行う。**

```
git fetch origin
git checkout -b <branch> origin/main
```

- **既存のブランチを再利用しない。** 作業を始める前に必ず現在のブランチを確認し、`main` にいるつもりで別のブランチにいた、あるいはその逆、という取り違えを防ぐ
- マージ済みのブランチの上に積まない。マージされた時点でそのブランチは終わりであり、次の作業は `main` から切り直す
- 変更は Pull Request で `main` に入れる。直接 push しない
- ブランチ名は `docs/` `chore/` `feat/` `fix/` の接頭辞を付ける

## 10. 作業のルール

- **タスクの境界を越えない。** 担当タスクのスコープ外のファイルを「ついでに」直さない。気づいた問題は報告する
- **完了を主張する前に証拠を出す。** 実行したコマンドとその出力を示す。テストを実行していないなら「実行していない」と書く
- **推測を事実として書かない。** 確認できていないことは「未確認」と明示する
- 実機でしか確認できないこと（動画の実描画、権限ダイアログの実挙動、スクロール性能）は、**自分で確認できないと明示して人間に上げる**
- コミットメッセージとコードのコメントは日本語で書く
- `.kiro/specs/*/requirements.md` `design.md` `tasks.md` は仕様であって実装メモではない。実装中に勝手に書き換えない
